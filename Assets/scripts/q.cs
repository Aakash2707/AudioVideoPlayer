using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

public class q : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {/*
        int max=0;
        int[] arr={10,2,3,54,48,89,69};
        for (int i=0;i<8;i++)
        {
            if(arr[i]<arr[i+1]);
            {
                int max=arr[i+1];
            }
              
        }
        Debug.Log(max);
        */
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
